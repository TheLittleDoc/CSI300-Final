/** @type {import('next').NextConfig} */
const nextConfig =
    {
        allowedDevOrigins: [
            'http://localhost:5000',
            'http://localhost:3000'
            ]
    };

export default nextConfig;
